namespace University.Enrollments.Domain
{
    /// <summary>
    /// Status de um enrollment (esboço sem lógica).
    /// </summary>
    public enum EnrollmentStatus
    {
        Requested,
        Enrolled,
        Dropped,
        Completed,
        Cancelled
    }
}
